import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
    title: "Login | RediStra",
    description: "Login to your RediStra dashboard",
}

export default function LoginPage() {
    return <LoginForm />
}
